# Build Checklist

## Build Preferences

- **Build mode:** Autonomous
- **Comprehension checks:** N/A (autonomous mode)
- **Git:** Commit after each item with message: "Complete step N: [title]"
- **Verification:** Yes — checkpoints every 3 items (after items 3, 6, 9, 12)
- **Check-in cadence:** N/A (autonomous mode)

## Checklist

- [x] **1. Project scaffold**
  Spec ref: `spec.md > Stack` + `spec.md > Runtime & Deployment` + `spec.md > File Structure`
  What to build: Scaffold the project using `npm create vite@latest` with the React template. Install and configure Tailwind CSS v4 (CSS-first — no `tailwind.config.js`, use `@import "tailwindcss"` and `@theme` in `index.css`). Install shadcn/ui via manual install. Create the full `src/` folder structure from spec: `screens/`, `components/ui/`, `components/`, `store/`, `logic/`, `data/`, `hooks/`. Create placeholder files (empty exports) for every file listed in the spec's file structure. Set up `index.css` with dark palette `@theme` variables (black/graphite base, electric cyan accent, muted gold for rewards). Create `public/manifest.json` with PWA fields (name: "Arise", display: "standalone", background_color, theme_color) and placeholder icons at `public/icons/icon-192.png` and `public/icons/icon-512.png`. Wire `App.jsx` to render a single dark div with "Arise" text as a placeholder. Update `vite.config.js` if needed for PWA or path aliases.
  Acceptance: App loads in browser at localhost:5173 with a dark background. No console errors. File structure matches spec exactly.
  Verify: Run `npm run dev` → confirm localhost:5173 shows a dark screen. Open DevTools → confirm no errors. Check `src/` folder structure matches spec.

- [x] **2. State foundation**
  Spec ref: `spec.md > Architecture Overview` + `spec.md > Data Model`
  What to build: Implement `store/actions.js` with all action type string constants (NAVIGATE, COMPLETE_ONBOARDING, START_PROGRAM, ADVANCE_DAY, LOG_QUEST_COMPLETION, SHOW_GUILT, GIVE_UP_TODAY, ABANDON_PROGRAM, and any others needed). Implement `store/localStorage.js` with read/write helpers for all 5 keys (`arise_userProfile`, `arise_programState`, `arise_dailyLog`, `arise_dailySnapshots`, `arise_guiltState`) and a `loadState()` function that returns all five or their defaults. Implement `store/reducer.js` with the full initial state shape matching the data model in spec and a switch statement with stubs for all action types — just enough to not throw errors. Implement `store/AppContext.jsx` with `createContext`, a `useReducer` hook initialized from `loadState()`, an effect that syncs full state to localStorage on every state change, and a Provider that wraps children. Update `App.jsx` to wrap everything in `AppContextProvider` and read `currentScreen` from context — render a placeholder `<div>Screen: {currentScreen}</div>` for now.
  Acceptance: Context wraps the app, initial state is loaded from localStorage on mount (or defaults to fresh state with `currentScreen: 'THRESHOLD'`), dispatching an action doesn't throw.
  Verify: Open browser → open DevTools → Application tab → localStorage → confirm `arise_programState` key exists with `currentScreen: "THRESHOLD"`. Refresh page and confirm the value persists.

- [x] **3. Logic layer**
  Spec ref: `spec.md > Logic`
  What to build: Implement all 5 pure functions with no React imports. `logic/xp.js`: `getTotalXP(dailyLog)`, `getWeeklyXP(dailyLog, currentDay)`, `getLevel(totalXP)` (`Math.floor(totalXP / 400) + 1`), `getXPToNextLevel(totalXP)` (`400 - (totalXP % 400)`). `logic/dayTransition.js`: `shouldTransition(storedDate)` — compare stored YYYY-MM-DD to today's date. `logic/statProjection.js`: implement the Day 66 projection formula from spec — `projectedXP` summing each habit's XP × frequency × 66; `projectedStats` summing stat gains × completions. `logic/questStatus.js`: implement `getQuestCards(userProfile, todayLog, now)` returning an array of quest card objects with fields: `questType`, `title`, `target`, `weeklyContext`, `status` (Done/Pending/At-Risk/Easy Win), `xp`, `statGains`, and `completionData`. Status logic: Done if logged in todayLog; Easy Win if incomplete + low-effort (wake, water, screen time) + not past trigger time; At-Risk if high-effort (cardio, training, reading) + time ≥ 9PM OR last day of week + 1 session remaining; Pending otherwise. `logic/rollback.js`: `checkRollback(programState, dailySnapshots)` — returns `{ shouldRollback: boolean, targetSnapshot }` based on `consecutiveFailureDays >= 3`.
  Acceptance: Each function returns the correct shape when called with valid inputs. Quest status logic correctly categorizes quests.
  Verify: **Verification checkpoint 1** — Open browser console. Paste and run: `import('./src/logic/questStatus.js').then(m => console.log(m.getQuestCards({ targetWakeTime: '06:00', dailyWaterTarget: 2, weeklyCardioSessions: 3, cardioDuration: 30, weeklyTrainingSessions: 3, trainingDuration: 30, dailyReadingPages: 20, dailyScreenTimeCap: 2, triggerTime: '20:00' }, {}, new Date())))`. Confirm it logs an array of 4–6 quest objects each with a `status` field. Then confirm `getLevel(0) === 1` and `getLevel(400) === 2`.

- [x] **4. Onboarding flow**
  Spec ref: `spec.md > Screens > ThresholdScreen` + `spec.md > Screens > OnboardingScreen`
  What to build: Implement `ThresholdScreen.jsx` — dark full-screen view, Arise mark centered, CSS keyframe ambient motion (slow pulse or subtle particle drift, no library), charged copy line "You are not starting over. You are starting awake.", single CTA button "Begin Assessment". On tap: dispatch `NAVIGATE` → `ONBOARDING`. Implement `OnboardingScreen.jsx` — renders one question at a time full screen, no progress bar. 9 questions in sequence: (1) Age slider 13–60, (2) Current life situation multiple choice (4 judgmental options), (3) Biggest reason for reset multiple choice (4 options), (4) Stress/pride self-assessment multiple choice, (5) Readiness multiple choice, (6) Ambition level multiple choice, (7) Target wake time slider 4:00–10:00 AM 15-min increments, (8) Quantitative habits (water liters, cardio sessions+duration, training sessions+duration, reading pages, screen-time cap) — multiple sliders on one screen, (9) Re-engagement trigger time slider 6:00 PM–11:00 PM 30-min increments. User cannot advance without selecting/moving. Write the judgmental option copy (e.g. "I've been saying 'I'll start Monday' for months") during this step. On completion: dispatch `COMPLETE_ONBOARDING` with all values → reducer saves to `arise_userProfile` → navigate to `PLAN_CONFIRMATION`.
  Acceptance: From prd.md — threshold screen shows charged copy and single CTA; questions appear one at a time with no progress bar; user cannot skip a question; onboarding saves all 13 data points to userProfile.
  Verify: Run dev server → open app → confirm threshold screen renders with ambient motion. Tap "Begin Assessment" → confirm first question appears. Complete all 9 questions → confirm localStorage `arise_userProfile` is written with all expected fields.

- [x] **5. Plan Confirmation + Commitment screens**
  Spec ref: `spec.md > Screens > PlanConfirmationScreen` + `spec.md > Screens > CommitmentScreen`
  What to build: Implement `PlanConfirmationScreen.jsx` — two-column layout: "You, Today" (Level 1, 0 XP, thin muted progress bar, "This is your starting point") vs "Day 66 Projection" (projected level + XP computed via `statProjection.js`, bright full bar, payoff copy). Five `StatRow` components (Wisdom, Confidence, Strength, Discipline, Focus) showing 0 → projected value; animate on mount — values count up from 0 using a CSS counter or `requestAnimationFrame`. Implement `StatRow.jsx` — props: `statName`, `currentValue`, `projectedValue`, `animate`. CTA: "Claim This Path" → navigate to `COMMITMENT`. Implement `CommitmentScreen.jsx` — full-screen dark background, short vow copy that acknowledges setbacks ("even when you slip"), single CTA "Start My Program". On tap: dispatch `START_PROGRAM` → sets Day 1 state (currentDay: 1, programStartDate: today, currentScreen: DASHBOARD) → navigate immediately to DASHBOARD.
  Acceptance: From prd.md — plan confirmation shows Today vs Day 66 with 5 animated stat rows; projected values reflect onboarding inputs; "Claim This Path" leads to commitment screen; "Start My Program" loads dashboard immediately with no delay.
  Verify: Complete onboarding → confirm plan confirmation screen shows with correct projected values (a user with 5 cardio sessions/week should project higher Strength than 2). Tap through to commitment → tap "Start My Program" → confirm `arise_programState.currentDay === 1` in localStorage and screen shows `DASHBOARD`.

- [x] **6. Dashboard shell + shared components**
  Spec ref: `spec.md > Screens > DashboardScreen` + `spec.md > Components > XPLevelBar` + `spec.md > Components > StreakIndicator` + `spec.md > Components > SystemMessage`
  What to build: Implement `DashboardScreen.jsx` skeleton — header section with "Day [N] of 66" prominent, `StreakIndicator` component, `XPLevelBar` component. Gear icon in header that dispatches `NAVIGATE` → `SETTINGS`. Placeholder body where quest cards will go (render "Quest cards loading..." text for now). Implement `XPLevelBar.jsx` — props: `currentXP`, `currentLevel`, `xpToNextLevel`. Shows level number, XP fraction (e.g. "240 / 400 XP"), animated bar fill via CSS transition on width. Implement `StreakIndicator.jsx` — props: `streakDays`, `lockedMilestones`, `currentDay`. Copy variants: "Day [N] streak — locked" if on milestone day and in lockedMilestones; "[N] days left to lock your [M]-day streak" if approaching next milestone within 3 days; "[N]-day streak" standard. Implement `SystemMessage.jsx` — props: `message`, `tone` (sarcasm | disappointment | pressure). Renders a styled chat bubble prefixed with "System:". Wire `App.jsx` screen controller to render `DashboardScreen` when `currentScreen === 'DASHBOARD'`.
  Acceptance: From prd.md — dashboard header shows Day N of 66, streak indicator with urgency copy, XP level bar with level number and bar fill. All update correctly when state changes.
  Verify: **Verification checkpoint 2** — Run dev server → complete onboarding → reach dashboard → confirm header shows "Day 1 of 66", streak indicator shows, XP bar shows Level 1. Open DevTools and manually update `arise_programState.totalXP` to 450 in localStorage → refresh → confirm level shows 2.

- [x] **7. Quest cards + all 5 logging flows + EvidenceStrip**
  Spec ref: `spec.md > Components > QuestCard` + `spec.md > Components > EvidenceStrip` + `spec.md > Logic > questStatus.js`
  What to build: Implement `QuestCard.jsx` fully. Props: `questType`, `questData`, `dispatch`. Renders: icon + quest title, exact target, weekly frequency context, status chip (Done green / Pending neutral / At-Risk amber / Easy Win cyan), XP reward + stat gains, logging CTA. Five distinct CTA behaviors: (1) Wake time — single tap "I woke at target time", dispatches immediately; (2) Water — shows running total vs target, "+ 0.5L" increment button, marks Done when target reached; (3) Cardio/Training — tap starts a countdown timer set to session duration; user can "End Early" (marks partial, no XP) or timer completes (marks Done, awards XP); (4) Reading — number input for pages read, accumulates toward daily target; (5) Screen time — manual number entry for hours, marks Done if ≤ cap. On completion: dispatch `LOG_QUEST_COMPLETION({ questType, value })` → reducer updates `dailyLog[currentDay]` + `totalXP` + `stats` → XP animates into XPLevelBar. Wire `DashboardScreen` to call `getQuestCards(userProfile, dailyLog[currentDay], now)` and render a `QuestCard` for each. Add `setInterval` every 60s to re-evaluate quest statuses. Implement `EvidenceStrip.jsx` — three always-visible data points: XP earned this week (from `getWeeklyXP`), stat deltas since Day 1, habit consistency % computed from dailyLog. Tone: 70% proof, 30% pressure.
  Acceptance: From prd.md — 4–6 quest cards render with correct status chips; each card's CTA matches its quest type; completing a quest shows Done chip and animates XP into bar; evidence strip shows correct values.
  Verify: On dashboard, tap the wake time CTA → confirm card shows Done chip and XP bar animates. Start a cardio timer → tap "End Early" → confirm partial state (no XP awarded). Log reading pages → confirm accumulation toward target.

- [x] **8. Day transition + milestone screens**
  Spec ref: `spec.md > Screens > DayTransitionScreen` + `spec.md > Screens > MilestoneScreen` + `spec.md > Hooks > useMidnightCheck.js`
  What to build: Implement `useMidnightCheck.js` — `setInterval` every 60s, calls `shouldTransition(programState.lastOpenDate)`, dispatches `ADVANCE_DAY` if true. Wire to `App.jsx` (or `DashboardScreen`). Implement `ADVANCE_DAY` reducer case: saves snapshot if `currentDay % 7 === 0`; applies rolled-over tasks from today's give-up or partials to tomorrow; resets quest completions; increments `currentDay`; checks `consecutiveFailureDays >= 3` → calls `checkRollback()` if true; navigates to `DAY_TRANSITION`. Implement `DayTransitionScreen.jsx` — branches on completion state: full completion (encouraging message + "Day [N] begins" + continue CTA) vs partial/give-up (rolled-over tasks listed + penalty note + continue CTA). If rollback triggered: show System message "I'm disappointed in you." + rollback details. On continue: navigate to `DASHBOARD`. Implement `MilestoneScreen.jsx` — shown once when currentDay is in [1, 7, 14, 21, 35, 48, 66] and not in `lockedMilestones`. Shows milestone name, "Streak Locked" state, identity-framing System message. Records milestone in `lockedMilestones`. On continue: navigate to `DASHBOARD`.
  Acceptance: From prd.md — transition screen shows on new day detection; branches correctly on completion vs partial; rolled-over tasks appear in tomorrow's quest cards; milestone screen shows once at each milestone day and never repeats.
  Verify: Manually set `arise_programState.lastOpenDate` to yesterday's date in localStorage → refresh app → confirm DayTransitionScreen appears. Complete all quests on Day 1 → confirm MilestoneScreen appears (Day 1 is a milestone day).

- [x] **9. Guilt system**
  Spec ref: `spec.md > Screens > GuiltScreen` + `spec.md > Guilt Messages` + `spec.md > Hooks > useGuiltTrigger.js`
  What to build: Implement `src/data/guiltMessages.js` — generate all 90 messages: 30 sarcasm (dry, wry, knowing), 30 disappointment (quiet, heavy, personal), 30 pressure (direct, urgent, unforgiving). Messages should sound like a System voice that knows you personally. Implement `useGuiltTrigger.js` — runs on DashboardScreen mount and every 5 minutes. Condition: current time ≥ `userProfile.triggerTime` AND quest completion < 50% (quests Done / total quests). If true: dispatch `SHOW_GUILT`. Implement `GuiltScreen.jsx` — replaces dashboard (full screen swap, not modal). Light-novel / visual novel chat interface: messages appear as "System:" bubbles using `SystemMessage.jsx`. Selection logic: pick tone at random (33% each), pick random index not in `guiltState.shownMessageIndices`, add to shown list. Snooze button available for first 3 encounters — dismisses for 30 min (set a timestamp, check on re-mount). On 4th encounter: snooze removed, two options only: "Give Up on Today" (dispatch `GIVE_UP_TODAY`, show "I'm disappointed in you." System message, navigate to DASHBOARD with give-up state) and "I'll Do It" (navigate to DASHBOARD). Wire `SHOW_GUILT` reducer case and `GIVE_UP_TODAY` case (marks `wasGiveUp: true`, increments `consecutiveFailureDays`, adds non-daily quests to tomorrow's rollovers).
  Acceptance: From prd.md — guilt screen replaces dashboard when trigger conditions met; messages cycle across 3 tones without immediate repeats; snooze works for first 3 encounters; 4th encounter shows only two options; "Give Up on Today" applies rolled-over tasks.
  Verify: **Verification checkpoint 3** — Manually set `triggerTime` to 1 minute from now in userProfile and clear today's completions → wait for trigger → confirm GuiltScreen appears. Snooze 3 times → confirm snooze button disappears on 4th encounter. Tap "Give Up on Today" → confirm DASHBOARD loads and `wasGiveUp: true` in dailyLog.

- [x] **10. Settings + rollback integration**
  Spec ref: `spec.md > Screens > SettingsScreen` + `spec.md > Logic > rollback.js`
  What to build: Implement `SettingsScreen.jsx` — accessible from dashboard gear icon. First visit (when `!programState.settingsUsed`): shows all adjustable targets (wake time, water, cardio duration, training duration, reading pages, screen-time cap, trigger time) with sliders matching onboarding input types, plus a Save button. On save: dispatch `SAVE_SETTINGS` → reducer updates `userProfile` + sets `settingsUsed: true`. After save (when `settingsUsed: true`): target-editing UI is gone; only option shown is "Give Up on Program" button. "Give Up on Program" → confirmation dialog using `SystemMessage` + "Are you sure?" + "Yes, I'm done" / "No, keep going". "Yes, I'm done" → dispatch `ABANDON_PROGRAM` → navigate to `THRESHOLD`. Wire rollback announcement into `DayTransitionScreen` — if rollback was applied (check programState for a `rollbackApplied` flag set by ADVANCE_DAY reducer), show System message: "I'm disappointed in you." + "Your progress has been rolled back to Day [N]." before the standard transition content.
  Acceptance: From prd.md — settings shows target editors on first visit; after saving, target editors are gone permanently; "Give Up on Program" shows confirmation with System message; confirming abandons program and returns to threshold; rollback is announced on next open after 3 consecutive failures.
  Verify: Navigate to settings → modify wake time → save → confirm settings no longer shows editors on re-open. Simulate 3 consecutive failures by setting `consecutiveFailureDays: 3` in localStorage → trigger midnight check → confirm rollback message appears on DayTransitionScreen.

- [x] **11. PWA + Vercel deploy**
  Spec ref: `spec.md > Runtime & Deployment`
  What to build: Replace placeholder PWA icons with real 192×192 and 512×512 pixel icons (dark background, "A" or Arise mark, matches app aesthetic). Finalize `public/manifest.json` — name: "Arise", short_name: "Arise", display: "standalone", start_url: "/", background_color matching dark palette, theme_color matching accent. Initialize git in the project root: `git init`, add `.gitignore` (node_modules, dist, .env), stage all files, commit with message "Initial commit: Arise scaffold". Add remote: `git remote add origin https://github.com/MeSharath/Arise.git`. Push: `git push -u origin main`. Connect Vercel to the GitHub repo (Sharath does this in the Vercel dashboard — agent provides the steps). Confirm auto-deploy triggers on push and live URL is accessible. Test "Add to Home Screen" on phone — confirm app launches full-screen with no browser chrome.
  Acceptance: From spec — Vercel URL is live, app loads correctly on mobile browser, "Add to Home Screen" installs the PWA, PWA launches in standalone mode.
  Verify: Open the Vercel URL on your phone → tap "Add to Home Screen" → launch from home screen → confirm app opens full-screen with no browser address bar. Confirm no console errors on mobile.

- [ ] **12. Submit your project to Devpost**
  Spec ref: `prd.md > What We're Building` (the core submission story)
  What to build: Walk through the Devpost submission form. Project name: "Arise — 66-Day Life Reset". Tagline: "Every habit app flatters you into quitting. Arise doesn't." Write the project story using scope.md and prd.md as source material: explain the core problem (habit apps that flatter instead of challenge), what Arise does differently (punitive rollback, guilt mechanics, RPG progression, finite 66-day arc), and the "wow moment" (the guilt screen — a dark visual-novel interface that confronts you by name). Add "built with" tags: React, Vite, Tailwind CSS, shadcn/ui, Vercel. Take screenshots: (1) ThresholdScreen with ambient motion, (2) Dashboard mid-program with quest cards in mixed states (Done/At-Risk/Easy Win), (3) GuiltScreen showing the light-novel chat interface, (4) PlanConfirmationScreen showing the animated Day 1 vs Day 66 stat comparison. Link the deployed Vercel URL. Link the GitHub repo at https://github.com/MeSharath/Arise.git. Review all fields and submit.
  Acceptance: Submission is live on Devpost with project name, tagline, description, built-with tags, screenshots of all 4 key moments, live Vercel URL, and GitHub repo link. All required fields complete.
  Verify: Open your Devpost submission page and confirm the green "Submitted" badge appears. Read the project description — would someone who knows nothing about Arise understand what it does and why it matters from the first two sentences?
